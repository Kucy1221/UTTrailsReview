<div class="container">
        <% obj.each do |review| %>
                <div class="row justify-content-md-center mt-4">
                    <div class="col-8">
                        <div class="card shadow" style="width: 100%;">
                          <div class="card-body">
                            <h5 class="card-title"><%= link_to review.title, review_path(review.id) %></h5>
                            <p class="card-text"><%= truncate(review.body, length: 100) %></p>
                            <%= link_to "Go to Review", review_path(review.id), class: "btn btn-primary" %>
                          </div>
                          <div class="card-footer text-muted">
                              <small> Created <%= time_ago_in_words(review.created_at) %> ago by <%= link_to review.user.username, user_path(review.user.id) %></small>
                          </div>
                        </div>
                    </div>
                </div>
        <% end %> 
    </div>